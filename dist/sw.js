/* Generated by build-pwa.mjs. Do not edit dist/sw.js directly. */
const VERSION = 'f1af3fc9bed652a0';
const PREFIX = 'ahorra-plus-' + encodeURIComponent(self.registration.scope) + '-';
const CACHE = PREFIX + VERSION;
const URLS = ["./assets/index-B_DahdZn.js","./assets/index-CllK5NvC.css","./fonts/OFL-Manrope.txt","./fonts/manrope-latin.woff2","./icons/apple-touch-icon.png","./icons/icon-192.png","./icons/icon-512.png","./icons/logo.png","./icons/logo.svg","./icons/maskable-512.png","./index.html","./manifest.webmanifest","./theme.js"].map(path => new URL(path, self.registration.scope).href);
const HOME = new URL('./index.html', self.registration.scope).href;

self.addEventListener('install', event => {
  // Installation is atomic: a missing asset leaves the previous app active.
  event.waitUntil(caches.open(CACHE).then(cache => cache.addAll(URLS)));
});
self.addEventListener('activate', event => {
  event.waitUntil((async () => {
    const names = await caches.keys();
    await Promise.all(names.filter(name => name.startsWith(PREFIX) && name !== CACHE).map(name => caches.delete(name)));
    await self.clients.claim();
  })());
});
self.addEventListener('message', event => {
  if (event.data?.type === 'SKIP_WAITING') self.skipWaiting();
});

self.addEventListener('push', event => {
  let data = {};
  try { data = event.data ? event.data.json() : {}; } catch { data = { body: event.data?.text() || 'Tienes un nuevo aviso.' }; }
  const title = data.title || 'Ahorra+';
  const options = {
    body: data.body || 'Tienes un nuevo aviso.',
    icon: data.icon || new URL('./icons/icon-192.png', self.registration.scope).href,
    badge: data.badge || new URL('./icons/icon-192.png', self.registration.scope).href,
    tag: data.tag || 'ahorra-plus',
    renotify: Boolean(data.renotify),
    requireInteraction: Boolean(data.requireInteraction),
    vibrate: data.vibrate || [100, 50, 100],
    data: data.data || { url: './' }
  };
  event.waitUntil(self.registration.showNotification(title, options));
});

self.addEventListener('notificationclick', event => {
  event.notification.close();
  const target = event.notification.data?.url || './';
  event.waitUntil((async () => {
    const absolute = new URL(target, self.registration.scope).href;
    const clients = await self.clients.matchAll({ type: 'window', includeUncontrolled: true });
    for (const client of clients) {
      if ('focus' in client) {
        await client.focus();
        if ('navigate' in client && client.url !== absolute) await client.navigate(absolute);
        return;
      }
    }
    if (self.clients.openWindow) await self.clients.openWindow(absolute);
  })());
});

self.addEventListener('fetch', event => {
  const request = event.request;
  const url = new URL(request.url);
  // Account data and sessions are never stored in the service worker cache.
  if (url.pathname.startsWith('/api/')) return;
  if (request.method !== 'GET' || url.origin !== self.location.origin || !url.href.startsWith(self.registration.scope)) return;
  event.respondWith((async () => {
    const cache = await caches.open(CACHE);
    // Use the matching shell version until the user applies an update.
    // Requests outside this app are never intercepted or cached.
    if (request.mode === 'navigate') {
      const home = await cache.match(HOME);
      if (home) return home;
    }
    const cached = await cache.match(request, { ignoreSearch: true });
    if (cached) return cached;
    try { return await fetch(request); }
    catch { return new Response('Este recurso no está disponible sin conexión.', { status: 503, headers: { 'Content-Type': 'text/plain; charset=utf-8' } }); }
  })());
});
